# 机器学习笔记

[TOC]

引言(Introduction)
------------------

### 1.1 机器学习是什么？

**Tom**定义的机器学习是，一个好的学习问题定义如下，他说，一个程序被认为能从经验**E**中学习，解决任务**T**，达到性能度量值**P**，当且仅当，有了经验**E**后，经过**P**评判，程序在处理T时的性能有所提升。

本课中，我希望教你有关各种不同类型的学习算法。目前存在几种不同类型的学习算法。主要的两种类型被我们称之为监督学习和无监督学习。在接下来的几个视频中，我会给出这些术语的定义。这里简单说两句，监督学习这个想法是指，我们将教计算机如何去完成任务，而在无监督学习中，我们打算让它自己进行学习。

### 1.3 监督学习

监督学习指的就是我们给学习算法一个数据集。这个数据集由“正确答案”组成。

监督学习又可以细分为：

1. 回归问题：回归问题指的是，我们试着推测出一个连续值的结果
2. 分类问题：分类问题指的是，我们试着推测出离散的输出值

#### 1.3.1 回归问题

##### 1.3.1.1 单变量线性回归（LR模型）（Linear Regression with One Variable）

>模型表示 : $h_\theta \left( x \right)=\theta_{0} + \theta_{1}x$
>
>> **参数**（**parameters**）: $\theta_{0}$ 和 $\theta_{1}$
>
>代价函数 : 平方误差代价函数
>
>> $J \left( \theta_0, \theta_1 \right) = \frac{1}{2m}\sum\limits_{i=1}^m \left( h_{\theta}(x^{(i)})-y^{(i)} \right)^{2}$
>
>优化算法 : (批量)梯度下降法（Batch Gradient Descent）
>
>> $\begin{aligned}&\text{repeat until convergence }\{\\&\theta_{j}:=\theta_{j}-\alpha\frac{\partial}{\partial\theta_{j}}J(\theta_{0},\theta_{1})\quad(\mathrm{for~}j=0\mathrm{~and~}j=1)\\&\}\end{aligned}$
>>
>> 具体：
>>
>>  ${\theta_{0}}:={\theta_{0}}-a\frac{1}{m}\sum\limits_{i=1}^{m}{ \left({{h}_{\theta }}({{x}^{(i)}})-{{y}^{(i)}} \right)}$
>>
>>  ${\theta_{1}}:={\theta_{1}}-a\frac{1}{m}\sum\limits_{i=1}^{m}{\left( \left({{h}_{\theta }}({{x}^{(i)}})-{{y}^{(i)}} \right)\cdot {{x}^{(i)}} \right)}$
>
>数据处理 : 特征缩放
>
>> + 均值归一化
>> + 最大最小归一化

##### 1.3.1.2 多变量线性回归（LR模型）（Linear Regression with Multiple Variables）

> 模型表示 : $h_{\theta}\left( x \right)=\theta^{T}X={\theta_{0}}+{\theta_{1}}{x_{1}}+{\theta_{2}}{x_{2}}+...+{\theta_{n}}{x_{n}}$
>
> >其中 $ x_{0} = 1 $
>
> 代价函数 : 平方误差代价函数
>
> > $J \left( \theta \right) = \frac{1}{2m}\sum\limits_{i=1}^m \left( h_{\theta}(x^{(i)})-y^{(i)} \right)^{2}$
>
> 优化算法 : (批量)梯度下降法（Batch Gradient Descent）
>
> > $\begin{aligned}&\text{repeat until convergence }\{\\&\theta_{j}:=\theta_{j}-\alpha\frac{\partial}{\partial\theta_{j}}J(\theta_{0},\theta_{1})\quad(\mathrm{for~}j=0,1,...,n)\\&\}\end{aligned}$
> >
> > 具体：
> >
> >  $\theta_{j}:={{\theta }_{j}}-a\frac{1}{m}\sum\limits_{i=1}^{m}{({{h}_{\theta }}({{x}^{(i)}})-{{y}^{(i)}})}x_{j}^{(i)}$
>
> 数据处理 : 特征缩放
>
> > + 均值归一化
> > + 最大最小归一化

#### 1.3.2 分类问题

##### 1.3.2.1 逻辑回归（Logistic回归）--二分类问题

>模型表示 : $h_\theta \left( x \right)=g\left(\theta^{T}X \right)=\frac{1}{1+{{e}^{-\theta^{T}X}}}$
>
>> $X$ 代表特征向量，即数据集
>> $g$ 代表逻辑函数（**logistic function**)是一个常用的逻辑函数为**S**形函数（**Sigmoid function**），公式为： $g\left( z \right)=\frac{1}{1+{{e}^{-z}}}$。
>
>> $h_\theta \left( x \right)$的作用是，对于给定的输入变量，根据选择的参数计算输出变量=1的可能性（**estimated probablity**）即$h_\theta \left( x \right)=P\left( y=1|x;\theta \right)$
>> 例如，如果对于给定的$x$，通过已经确定的参数计算得出$h_\theta \left( x \right)=0.7$，则表示有70%的几率$y$为正向类，相应地$y$为负向类的几率为1-0.7=0.3。(这是因为我们设置正向数据为1，负向数据为0)
>
>> ps ：对比线性模型 $h_{\theta}\left( x \right)=\theta^{T}X$ ,逻辑模型是在线性模型的基础上做了逻辑函数的映射。
>
>决策边界理解：$h_\theta \left( x \right)=g\left(f(x) \right)$, 决策边界即$f(x)$
>
>> 找到一个$f(x)$, 使得$f(x)>=0$的为一类, $f(x)<=0$的为一类。
>>
>> 而$f(x)$需要自己假设，可以是线性回归函数，也可以是圆形、椭圆、二次函数、高次函数等等。
>>
>> 例如$f(x)={\theta_0}+{\theta_1}{x_1}+{\theta_{2}}{x_{2}}+{\theta_{3}}x_{1}^{2}+{\theta_{4}}x_{2}^{2}+{\theta_{5}}x_{1}^{3}+{\theta_{6}}x_{2}^{3}+...$
>>
>> 具体参考：6.3节
>
>代价函数：$J\left( \theta  \right)=\frac{1}{m}\sum\limits_{i=1}^{m}{{Cost}\left( {h_\theta}\left( {x}^{\left( i \right)} \right),{y}^{\left( i \right)} \right)}$
>
>> 其中 $\operatorname{Cost}(h_\theta(x),y)=\left\{\begin{array}{rcl}-\log(h_\theta(x))&\text{if }y=1\\-\log(1-h_\theta(x))&\text{if }y=0\end{array}\right.$
>>
>> 或者：$Cost\left( {h_\theta}\left( x \right),y \right)=-y\times log\left( {h_\theta}\left( x \right) \right)-(1-y)\times log\left( 1-{h_\theta}\left( x \right) \right)$
>>
>> 注：逻辑回归中y的标签只有1或者0，所以Cost是一个分段函数，计算时没有y变量，是依靠y进行选择计算
>>
>> 注：$h_\theta \left( x \right)=g\left( z \right)=\frac{1}{1+{{e}^{-z}}}$, 其取值范围是(0, 1)，故$Cost()$函数取值范围是(0, 正无穷), $h_\theta \left( x \right)$越接近0，Cost()函数越接近正无穷，$h_\theta \left( x \right)$越接近1，Cost()函数越接近0
>>
>> 即：这样构建的$Cost\left( {h_\theta}\left( x \right),y \right)$函数的特点是：当实际的  $y=1$ 且${h_\theta}\left( x \right)$也为 1 时误差为 0，当 $y=1$ 但${h_\theta}\left( x \right)$不为1时误差随着${h_\theta}\left( x \right)$变小而变大；当实际的 $y=0$ 且${h_\theta}\left( x \right)$也为 0 时代价为 0，当$y=0$ 但${h_\theta}\left( x \right)$不为 0时误差随着 ${h_\theta}\left( x \right)$的变大而变大。
>
>> 此处代价函数不用平方误差代价函数的原因：
>>
>> 对于线性回归模型，我们定义的代价函数是所有模型误差的平方和。理论上来说，我们也可以对逻辑回归模型沿用这个定义，但是问题在于，当我们将${h_\theta}\left( x \right)=\frac{1}{1+{e^{-\theta^{T}x}}}$带入到这样定义了的代价函数中时，我们得到的代价函数将是一个非凸函数（**non-convexfunction**）。
>
>优化算法 : (批量)梯度下降法（Batch Gradient Descent）
>
>> $\begin{aligned}&\text{repeat until convergence }\{\\&\theta_{j}:=\theta_{j}-\alpha\frac{\partial}{\partial\theta_{j}}J(\theta_{0},\theta_{1})\quad(\mathrm{for~}j=0,1,...,n)\\&\}\end{aligned}$
>>
>> 具体：
>>
>> ${{\theta }_{j}}:={{\theta }_{j}}-a\frac{1}{m}\sum\limits_{i=1}^{m}{({{h}_{\theta }}({{x}^{(i)}})-{{y}^{(i)}})}x_{j}^{(i)}$
>>
>> > 该梯度下降形式与多变量线性回归一样，但是内部确不一样，具体推导见6.4节
>
>数据处理 : 特征缩放
>
>> + 均值归一化
>> + 最大最小归一化 

##### 1.3.2.2 多类别分类：一对多

> 训练模型选择：逻辑回归等分类算法
>
> 模型训练逻辑：
>
> 1. 为了能实现这样的转变，我们将多个类中的一个类标记为正向类（$y=1$），然后将其他所有类都标记为负向类，这个模型记作$h_\theta^{\left( 1 \right)}\left( x \right)$。
> 2. 接着，类似地第我们选择另一个类标记为正向类（$y=2$），再将其它类都标记为负向类，将这个模型记作 $h_\theta^{\left( 2 \right)}\left( x \right)$,依此类推。
> 3. 最后我们得到一系列的模型简记为： $h_\theta^{\left( i \right)}\left( x \right)=p\left( y=i|x;\theta  \right)$其中：$i=\left( 1,2,3....k \right)$ 
> 4. 最后，在我们需要做预测时，我们将所有的分类机都运行一遍，然后对每一个输入变量，都选择最高可能性的输出变量。
>
> 理解：总之，我们已经把要做的做完了，现在要做的就是训练这个逻辑回归分类器：$h_\theta^{\left( i \right)}\left( x \right)$， 其中 $i$ 对应每一个可能的 $y=i$，最后，为了做出预测，我们给出输入一个新的 $x$ 值，用这个做预测。我们要做的就是在我们三个分类器里面输入 $x$，然后我们选择一个让 $h_\theta^{\left( i \right)}\left( x \right)$ 最大的$ i$，即$\mathop{\max}\limits_i\,h_\theta^{\left( i \right)}\left( x \right)$。
>
> 你现在知道了基本的挑选分类器的方法，选择出哪一个分类器是可信度最高效果最好的，那么就可认为得到一个正确的分类，无论$i$值是多少，我们都有最高的概率值，我们预测$y$就是那个值。这就是多类别分类问题，以及一对多的方法，通过这个小方法，你现在也可以将逻辑回归分类器用在多类分类的问题上。

##### 1.3.2.3 神经网络

> 为什么要学习神经网络？
>
> 我们之前学的，无论是线性回归还是逻辑回归都有这样一个缺点，即：当特征太多时，计算的负荷会非常大。假设我们有非常多的特征，例如大于100个变量，我们希望用这100个特征来构建一个非线性的多项式模型，结果将是数量非常惊人的特征组合，即便我们只采用两两特征的组合$(x_1x_2+x_1x_3+x_1x_4+...+x_2x_3+x_2x_4+...+x_{99}x_{100})$，我们也会有接近5000个组合而成的特征。这对于一般的逻辑回归来说需要计算的特征太多了。普通的逻辑回归模型，不能有效地处理这么多的特征，这时候我们需要神经网络。

> 一些定义：神经网络模型建立在很多神经元之上，每一个神经元又是一个个学习模型。神经网络模型是许多逻辑单元按照不同层级组织起来的网络，每一层的输出变量都是下一层的输入变量。（参考8.3节）
>
> 激活单元，**activation unit**
>
> 参数又可被成为权重（**weight**）
>
> 输入单元（**input units**）
>
> 输出单元（**output units**）
>
> 第一层成为输入层（**Input Layer**）
>
> 最后一层称为输出层（**Output Layer**）
>
> 中间一层成为隐藏层（**Hidden Layers**）
>
> 每一层都可以增加一个偏差单位（**bias unit**）: $x_{0} = 1$, 用来控制逻辑回归中参数 $\theta_{0}$
>
> $a_{i}^{\left( j \right)}$ 代表第$j$ 层的第 $i$ 个激活单元
>
> ${{\theta }^{\left( j \right)}}$代表从第 $j$ 层映射到第$ j+1$ 层时的权重的矩阵，例如${{\theta }^{\left( 1 \right)}}$代表从第一层映射到第二层的权重的矩阵。其尺寸为：以第 $j+1$层的激活单元数量为行数，以第 $j$ 层的激活单元数加一为列数的矩阵。

> 前向传播算法：参考8.4、8.5、8.6节
>
> 例子 ：实现逻辑与(**AND**)、逻辑或(**OR**)、逻辑非（**NOT**）、**XNOR** 功能（输入的两个值必须一样，均为1或均为0）

> 多类分类：参考8.7节
>
> 如果有n个类别输出，可以在神经网络输出层设置n个神经元，每个输出神经元代表一个逻辑回归分类，从而输出一个n*1维的向量表示当前输入属于什么类别，类比逻辑回归的多分类，相当于在最后一层设置了多个逻辑回归分类。

##### 1.3.2.4 神经网络的学习

> 模型表示 : 
>
> > 参考8.4节
> >
> > $a^{(0)}=x$
> >
> > $\mathsf{Add}a_{0}^{(0)}=1$
> >
> > when i>=1 and i<=L:
> >
> > ​	$z^{(i)}=\Theta^{(i-1)}a^{(i-1)}$
> >
> > ​	$a^{(i)}=g(z^{(i)})$
> >
> > ​	$\mathsf{Add}a_{0}^{(i)}=1$
> >
> > $h_{\Theta}(x)=a^{(L)}=g(z^{(L)})$
>
> 代价函数 : 
>
> > 我们回顾逻辑回归问题中我们的代价函数为：
> >
> > $  J\left(\theta \right)=-\frac{1}{m}\left[\sum_\limits{i=1}^{m}{y}^{(i)}\log{h_\theta({x}^{(i)})}+\left(1-{y}^{(i)}\right)log\left(1-h_\theta\left({x}^{(i)}\right)\right)\right]+\frac{\lambda}{2m}\sum_\limits{j=1}^{n}{\theta_j}^{2}  $
> >
> > 在逻辑回归中，我们只有一个输出变量，又称标量（**scalar**），也只有一个因变量$y$，但是在神经网络中，我们可以有很多输出变量，我们的$h_\theta(x)$是一个维度为$K$的向量，并且我们训练集中的因变量也是同样维度的一个向量，因此我们的代价函数会比逻辑回归更加复杂一些，为：$\newcommand{\subk}[1]{ #1_k }$
> > $$h_\theta\left(x\right)\in \mathbb{R}^{K}$$ $${\left({h_\theta}\left(x\right)\right)}_{i}={i}^{th} \text{output}$$
> >
> > $J(\Theta) = -\frac{1}{m} \left[ \sum\limits_{i=1}^{m} \sum\limits_{k=1}^{k} {y_k}^{(i)} \log \subk{(h_\Theta(x^{(i)}))} + \left( 1 - y_k^{(i)} \right) \log \left( 1- \subk{\left( h_\Theta \left( x^{(i)} \right) \right)} \right) \right] + \frac{\lambda}{2m} \sum\limits_{l=1}^{L-1} \sum\limits_{i=1}^{s_l} \sum\limits_{j=1}^{s_{l+1}} \left( \Theta_{ji}^{(l)} \right)^2$
> >
> > + 第一项的含义是，首先有m个输入样本，所以有m个输出值，对每一个输入样本的k输出值做一个误差累加，再对所有m个样本的每一个误差项做一个累加求均值，表示代价函数的值。
> > + 正则项有三层嵌套是由于：逻辑回归只相当于单个神经元单个输出，所以只有一个$\Theta$，所以只需要一个嵌套，而神经网络相当于多层逻辑回归（加一层嵌套），并且输出是n维输出（再加一层嵌套），故其正则化加上本身逻辑回归的嵌套，总共有三层嵌套。
> > + ps : 由于每个隐藏层有$S_{(i)}$个神经元，但是还包括一个偏差神经元$\theta^{(i)}_{0}$，所以每层神经元的格式遍历实际上为$i=[0:S_{(i)}]$，但是该代价函数（包括正则项）中没有计算偏差神经元的误差（一般是不用加的），这是由于偏差神经元都为1，不影响总体误差。但是也可以加上，对总体没有影响。
>
> 反向传播算法
>
> > 首先通过下式计算出所有的 $\delta_{i}^{l}$
> >
> > $$\delta_{i}^{(l)}=\begin{cases}(a_{i}^{(n_{l})}-y_{i})f^{\prime}(z_{i}^{(n_{l})}),&\text{当 }l=n_{l}\text{时}.\\(\sum_{k=1}^{S_{l+1}}W_{ki}^{(l)}\cdot\delta_{k}^{(l+1)})\cdot f^{\prime}(z_{i}^{(l)}),&\text{当 }n_{l}>l\geq1\text{时}.\end{cases}$$
> >
> > 而后通过下式计算出所有的整体代价函数 $J(W,b)$ 关于 $W_{ij}^{l}$ 和 $b_{i}^{l}$ 的偏导数
> >
> > $$\begin{cases}
> > \frac{\partial J(W,b)}{\partial W_{ij}^{(l)}}=
> > [\frac{1}{m}\sum_{t=1}^{m}a_{j}^{(l)}\cdot\delta_i^{(l+1)}(x^{(t)},y^{(t)})]+\lambda W_{ij}^{(l)}\\
> > \frac{\partial J(W,b)}{\partial b_i^{(l)}}=[\frac{1}{m}\sum_{t=1}^{m}\delta_i^{(l+1)}(x^{(t)},y^{(t)})]
> > \end{cases}$$
>
> 梯度检验
>
> > 当我们对一个较为复杂的模型（例如神经网络）使用梯度下降算法时，可能会存在一些不容易察觉的错误，意味着，虽然代价看上去在不断减小，但最终的结果可能并不是最优解。为了避免这样的问题，我们采取一种叫做梯度的数值检验（**Numerical Gradient Checking**）方法。这种方法的思想是通过估计梯度值来检验我们计算的导数值是否真的是我们要求的。
> >
> > 对梯度的估计采用的方法是在代价函数上沿着切线的方向选择离两个非常近的点然后计算两个点的平均值用以估计梯度。即对于某个特定的 $\theta$，我们计算出在 $\theta$-$\varepsilon $ 处和 $\theta$+$\varepsilon $ 的代价值（$\varepsilon $是一个非常小的值，通常选取 0.001），然后求两个代价的平均，用以估计在 $\theta$ 处的代价值。
> >
> > 具体实现：
> >
> > $gradApprox = (J(theta + eps) – J(theta - eps)) / (2*eps)$
> >
> > 当$\theta$是一个向量时，我们则需要对偏导数进行检验。因为代价函数的偏导数检验只针对一个参数的改变进行检验，下面是一个只针对$\theta_1$进行检验的示例：
> > $$ \frac{\partial}{\partial\theta_1}=\frac{J\left(\theta_1+\varepsilon_1,\theta_2,\theta_3...\theta_n \right)-J \left( \theta_1-\varepsilon_1,\theta_2,\theta_3...\theta_n \right)}{2\varepsilon} $$
> >
> > 若梯度检验得到的梯度与反向传播得到的梯度类似，表明梯度检验通过，可以进行梯度下降算法。
>
> 优化算法 : (批量)梯度下降法（Batch Gradient Descent）
>
> > $W_{ij}^{(l)}:=W_{ij}^{(l)}-\alpha\frac{\partial}{\partial W_{ij}^{(l)}}J(W,b)\quad(\mathrm{for~}l=1,...,n_{l})$
> >
> > 再进行反向传播算法+梯度检验
> >
> > 而后继续进行梯度下降
> >
> > 重复上述步骤直至达到停机准则
>
> 数据处理 : 特征缩放
>
> > + 均值归一化
> > + 最大最小归一化
>
> 总结：
>
> > 训练神经网络：我们真正要决定的是隐藏层的层数和每个中间层的单元数。
> >
> > 1. 参数的随机初始化
> >
> > 2. 利用正向传播方法计算所有的$h_{\theta}(x)$
> >
> > 3. 编写计算代价函数 $J$ 的代码
> >
> > 4. 利用反向传播方法计算所有偏导数
> >
> > 5. 利用数值检验方法检验这些偏导数
> >
> > 6. 使用优化算法来最小化代价函数

### 1.4 无监督学习

无监督学习中没有任何的标签或者是有相同的标签或者就是没标签。所以我们已知数据集，却不知如何处理，也未告知每个数据点是什么。别的都不知道，就是一个数据集。

算法：

+ 聚类算法

### 1.5 通用方法

#### 1.5.1 优化算法

+ 梯度下降
+ **共轭梯度法**
+ **BFGS** (**变尺度法**)
+ **L-BFGS** (**限制变尺度法**) 

> 这三种算法只需要提供代价函数，以及对应的偏导数形式，代入进去即可

#### 1.5.2 过拟合(**over-fitting**)与欠拟合问题

+ 过拟合：指模型对于训练数据拟合呈现过当的情况，反映到评估指标上，就是模型在训练集上的表现很好，但是在测试集和新数据上的表现较差。导致模型泛化能力较差。

> 解决方法：
>
> + 从数据入手，获得更多的训练数据。
>
> > 直接增加数据，或者对原有数据做一些扩充，比如图像翻转、平移、缩放等方式，或者使用生成对抗网络来合成大量的新数据。
>
> + 丢弃一些不能帮助我们正确预测的特征。可以是手工选择保留哪些特征，或者使用一些模型选择的算法来帮忙（例如**PCA**）
> + 降低模型复杂度
> + 正则化方法
> + 集成学习方法

+ 欠拟合：指的是模型没有很好的捕捉到数据的特征，不能够很好的拟合数据，指的是模型在训练和预测时表现都不好的情况。 

> 解决方法：
>
> + 添加新特征
>
> > 比如特征组合等
>
> + 增加模型复杂度
> + 减小正则化系数

#### 1.5.3 模型+正则化

假如我们有非常多的特征，我们并不知道其中哪些特征我们要惩罚，我们将对所有的特征进行惩罚，并且让代价函数最优化的软件来选择这些惩罚的程度。这样的结果是得到了一个较为简单的能防止过拟合问题的假设：
$$
J\left( \theta  \right)=\frac{1}{2m}[\sum\limits_{i=1}^{m}{{{({h_\theta}({{x}^{(i)}})-{{y}^{(i)}})}^{2}}+\lambda \sum\limits_{j=1}^{n}{\theta_{j}^{2}}]}
$$
其中$\lambda $又称为正则化参数（**Regularization Parameter**）。 注：根据惯例，我们不对${\theta_{0}}$ 进行惩罚。

+ 正则化线性回归

> 模型表示 : $h_{\theta}\left( x \right)=\theta^{T}X={\theta_{0}}+{\theta_{1}}{x_{1}}+{\theta_{2}}{x_{2}}+...+{\theta_{n}}{x_{n}}$
>
> >其中 $ x_{0} = 1 $
>
> 代价函数 : 平方误差代价函数
>
> > $J\left( \theta  \right)=\frac{1}{2m}[\sum\limits_{i=1}^{m}{({{({h_\theta}({{x}^{(i)}})-{{y}^{(i)}})}^{2}}+\lambda \sum\limits_{j=1}^{n}{\theta _{j}^{2}}]}$
>
> 优化算法 : (批量)梯度下降法（Batch Gradient Descent）
>
> > ${\theta_0}:={\theta_0}-a\frac{1}{m}\sum\limits_{i=1}^{m}{(({h_\theta}({{x}^{(i)}})-{{y}^{(i)}})x_{0}^{(i)}})$                               ${\theta_j}:={\theta_j}-a[\frac{1}{m}\sum\limits_{i=1}^{m}{(({h_\theta}({{x}^{(i)}})-{{y}^{(i)}})x_{j}^{\left( i \right)}}+\frac{\lambda }{m}{\theta_j}]$ $for$ $j=1,2,...n$

+ 正则化的逻辑回归模型

> 模型表示 : $h_\theta \left( x \right)=g\left(\theta^{T}X \right)=\frac{1}{1+{{e}^{-\theta^{T}X}}}$
>
> 代价函数：$J\left( \theta  \right)=\frac{1}{m}\sum\limits_{i=1}^{m}{[-{{y}^{(i)}}\log \left( {h_\theta}\left( {{x}^{(i)}} \right) \right)-\left( 1-{{y}^{(i)}} \right)\log \left( 1-{h_\theta}\left( {{x}^{(i)}} \right) \right)]}+\frac{\lambda }{2m}\sum\limits_{j=1}^{n}{\theta _{j}^{2}}$
>
> 优化算法 : (批量)梯度下降法（Batch Gradient Descent）
>
> > $Repeat$  $until$  $convergence${
> >
> > ${\theta_0}:={\theta_0}-a\frac{1}{m}\sum\limits_{i=1}^{m}{(({h_\theta}({{x}^{(i)}})-{{y}^{(i)}})x_{0}^{(i)}})$
> >
> > ${\theta_j}:={\theta_j}-a[\frac{1}{m}\sum\limits_{i=1}^{m}{({h_\theta}({{x}^{(i)}})-{{y}^{(i)}})x_{j}^{\left( i \right)}}+\frac{\lambda }{m}{\theta_j}]$$for$ $j=1,2,...n$
> >
> > }

#### 1.5.4  算法诊断方法

当我们运用训练好了的模型来预测未知数据的时候发现有较大的误差，我们下一步可以做什么？

1. 获得更多的训练样本——可以解决高方差

2. 尝试减少特征的数量——可以解决高方差

3. 尝试获得更多的特征——可以解决高偏差

4. 尝试增加多项式特征——可以解决高偏差

5. 尝试减少正则化程度$\lambda$——可以解决高偏差

6. 尝试增加正则化程度$\lambda$——可以解决高方差

我们不应该随机选择上面的某种方法来改进我们的算法，而是运用一些机器学习诊断法来帮助我们知道上面哪些方法对我们的算法是有效的。

#### 1.5.5 划分训练集、验证集、测试集

具体参考10.2、10.3节

如果模型只有一个，则只需要划分训练集和测试集，一般划分规则如下：

+ 为了检验算法是否过拟合，我们将数据随机分成训练集和测试集，通常用70%的数据作为训练集，用剩下30%的数据作为测试集。

假设我们要在10个不同次数的二项式模型之间进行选择：这时候需要划分训练集、验证集、测试集来进行划分：

+ 使用60%的数据作为训练集，使用 20%的数据作为交叉验证集，使用20%的数据作为测试集

> ps ：此处验证集的作用是，首先将10个模型对训练集进行训练，获得每个模型的参数 $\theta^{(i)}$，然后将 $\theta^{(i)}$代入验证集求其代价函数，获得10个模型对应的验证集代价函数值，选取最小的代价函数模型的参数 $\theta^{(j)}$为最终模型参数，在测试集上测试模型效果。

#### 1.5.6 偏差和方差

参考视频10.4、10.5节

当你运行一个学习算法时，如果这个算法的表现不理想，那么多半是出现两种情况：要么是偏差比较大，要么是方差比较大。换句话说，出现的情况要么是欠拟合，要么是过拟合问题。

+ 高偏差对应欠拟合
+ 高方差对应过拟合

**多项式模型次数问题**：当选择使用不同的d次多项式拟合模型，并按照d作为横坐标，训练集误差和验证集误差作为纵坐标绘制图像，结果如下

![image-20240829140041549](C:\Users\Scar\AppData\Roaming\Typora\typora-user-images\image-20240829140041549.png)

+ 高偏差(high Bias)(欠拟合underfit)：训练误差和验证误差都较大，验证集误差与训练误差类似
+ 高方差(high Variance)(过拟合overfit)：训练误差非常小，但是验证集误差比较大，远大于训练误差

**正则化参数 $\lambda$ 的选择问题**：在训练模型的过程中，一般会使用一些正则化方法来防止过拟合， $\lambda$ 值的选择通常是($0,0.01,0.02,0.04,0.08,0.15,0.32,0.64,1.28,2.56,5.12,10$)。 我们同样把数据分为训练集、交叉验证集和测试集。

选择$\lambda$的方法为：

1. 使用训练集训练出12个不同程度正则化的模型
2. 用12个模型分别对交叉验证集计算的出交叉验证误差
3. 选择得出交叉验证误差**最小**的模型
4. 运用步骤3中选出模型对测试集计算得出测试误差。

我们也可以同时将12个模型的训练集和交叉验证集模型的代价函数误差与λ的值绘制在一张图表上：

![image-20240829141422714](C:\Users\Scar\AppData\Roaming\Typora\typora-user-images\image-20240829141422714.png)

+ 当 $\lambda$ 较小时，训练集误差较小（过拟合）而交叉验证集误差较大（此时为高方差）
+ 随着 $\lambda$ 的增加，训练集误差不断增加（欠拟合），而交叉验证集误差则是先减小后增加（最右侧为高偏差）

#### 1.5.7 学习曲线

参考视频10.6节

使用学习曲线来判断某一个学习算法是否处于偏差、方差问题。学习曲线是学习算法的一个很好的**合理检验**（**sanity check**）。

学习曲线：将样本数量(m)作为自变量(横坐标)，将训练集误差和交叉验证集误差作为因变量（纵坐标），绘制曲线。

思考：当样本数量(m)较少时，训练误差会比较小，验证误差会比较大；随着m增大，训练误差会越来越大，直到收敛，验证误差会越来越小，直到收敛；但是训练误差大概率会比验证误差更小（直观感觉，没有证明）。

所以学习曲线一般图像如下：

![image-20240829142316523](C:\Users\Scar\AppData\Roaming\Typora\typora-user-images\image-20240829142316523.png)

考虑**高偏差/欠拟合**的例子：尝试用一条直线来适应下面的数据，可以看出，无论训练集有多么大误差都不会有太大改观

![image-20240829142441318](C:\Users\Scar\AppData\Roaming\Typora\typora-user-images\image-20240829142441318.png)

+ 此时训练集误差与验证集误差接近，增加数据到训练集不一定能有帮助。

考虑**高方差/过拟合**的例子：假设我们使用一个非常高次的多项式模型，并且正则化非常小。

![image-20240829142619064](C:\Users\Scar\AppData\Roaming\Typora\typora-user-images\image-20240829142619064.png)

+ 此时交叉验证集误差远大于训练集误差，往训练集增加更多数据可以提高模型的效果。

> 可以通过绘制学习曲线去判断该模型是高方差还是高偏差问题，从而根据问题去选择解决方案（见1.5.4算法诊断一节）

**神经网络的方差和偏差**：

+ 使用较小的神经网络，类似于参数较少的情况，容易导致高偏差和欠拟合，但计算代价较小
+ 使用较大的神经网络，类似于参数较多的情况，容易导致高方差和过拟合，虽然计算代价比较大，但是可以通过正则化手段来调整而更加适应数据。
+ 通常选择较大的神经网络并采用正则化处理会比采用较小的神经网络效果要好。
+ 对于神经网络中的隐藏层的层数的选择，通常从一层开始逐渐增加层数，为了更好地作选择，可以把数据分为训练集、交叉验证集和测试集，针对不同隐藏层层数的神经网络训练神经网络，然后选择交叉验证集代价最小的神经网络。

